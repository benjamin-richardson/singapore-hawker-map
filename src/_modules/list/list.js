'use strict';

import $ from 'jquery';
import ListJs from 'list.js';

export default class List {
  constructor(node, options, values) {

    this.list = new ListJs(node, options, values);

  }

  getItem(item) {

  }

}
