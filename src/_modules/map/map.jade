mixin map(spec)
  - spec = spec || {}
  - spec.__class = spec.__class || ''
  - spec.attributes = spec.attributes || ''

  div(class=spec.__class)&attributes(spec.attributes)

    include ../../_images/test.svg
